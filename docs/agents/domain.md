# Domain documentation

SoundHub currently uses a single domain context.

Before changing domain behavior:

1. Read the root `CONTEXT.md` and use its canonical terms.
2. Read relevant accepted records in `docs/adr/`.
3. Surface conflicts with an ADR instead of silently overriding it.

The legacy records under `docs/architecture/` remain historical references; their status indicates
whether they are still authoritative.
